v = *p++     p가 가리키는 값을 v에 대입한 후에 p를 증가한다.v = 100,p = 34번지
 v = (*p)++  p가 가리키는 값을 v에 대입한 후에 가리키는 값을 증가한다.v = 100, p = 30번지   30번지의 값은 101
 v = *++p    p주소를 증가시킨 후에 p가 가리키는 값을 v에 대입한다.v = 500, p = 34번지
 v = ++*p    p가 가리키는 값을 가져온 후에 그 값을 증가하여 v에 대입한다.v 101, p = 30번지
 
int a = 100;
int *p;
p = &a; a의 주소는 30번지
        34번지에는 500 값 저장
